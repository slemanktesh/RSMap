pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        maven {
            url = 'https://repo.runelite.net'
        }
    }
}

rootProject.name = 'rsmap-tile-generator'
